# Матрица проверки и сдачи

## Актуальная проверка 23.09.2026, после интеграции UI

Этот раздел заменяет прежние статусы ниже; нижние разделы сохранены как история проверок.

По предоставленному Али Положению: п. 5.4.13 фиксирует итоговую версию в репозитории в 18:00 по Астане; пп. 5.4.15–5.4.16 требуют самостоятельного запуска строго по README; п. 5.6.6 требует проверки ключевых функций без личных аккаунтов и подписок участников. Путь проверки серверного сценария без ключа теперь описан в README. Живой AI без серверного ключа недоступен: демонстрационный доступ или иной способ проверки экспертами ещё нужно согласовать. Запись двух реальных ответов не заменяет живой доступ.

| Требование | Фактический результат |
| --- | --- |
| Реальный AI | Два примера, по 5 уместных вопросов и полная карточка, 4 HTTP 200 от реальных AI-маршрутов |
| Отсутствие выдумки | Не выполнено в «Ритейле»: пользователь результата ошибочно стал контактом; к отчёту добавлены рекомендации. Исправлено вручную перед публикацией. В «Образовании» неподтверждённых добавлений не обнаружено; возвращено пропущенное упоминание формы записи |
| Редактирование и подтверждение | Два полных браузерных пути с реальным AI, редактированием и подтверждением трёх полей |
| Рейтинг | HTTP 0→100; браузер 0→30, видна разбивка |
| Низкий рейтинг и каталог | Публикация при 0 через HTTP и при 30 в браузере; задачи видны, принимают отклики; тема и уровень применены совместно |
| Решения и этап | Два новых отклика вручную выбраны одновременно; один этап +10, повтор HTTP не увеличивает баллы. Во втором сценарии выбран один отклик. Конкурентные подтверждения покрыты серверным тестом |
| Seed и сохранение | По 5 записей каждого требуемого вида в SQLite до действий пользователя; перезапуск сохранил записи и не повторил seed |
| Ошибки | Неверный ввод, URL, отсутствие ключа — проверены. `openai.APITimeoutError` при подмене SDK пока даёт ошибочный `AI_UNAVAILABLE` вместо `AI_TIMEOUT`; исправление за Тимуром |
| Свежий клон | main `1a4d8cf587f113ea00e7e9724e199669dba4b649`, Windows Python 3.12.14, новая .venv, зависимости, pip check, 4 серверных + 8 AI-тестов; Uvicorn по README с --reload, home/health HTTP 200 |
| Последующее обновление UI | Отчёт и diff Дастана `a1e2693` изучены: только его web/app.js и статус, сохранение сообщений после действий. Merge `7fc698e`; повторно 4+8 тестов и проверка синтаксиса JS прошли |
| Демо и подача | Полный путь проверен без замера длительности. План Дастана 4:50 ещё не подтверждён таймером. Фактической подачи нет |

Синтетические входы, вопросы, исходные карточки, ручные исправления и токены: `data/live-ai-eval-2026-09-23.json`. Первый браузерный пример на `643c241`, второй на `1a4d8cf`. Это переписанные из UI результаты, не сырые трассировки API. Новых реальных запросов после двух примеров не выполняли.

Модель `gpt-4.1-mini-2025-04-14` доступна в аккаунте интеграции. 1154 входных и 498 выходных токенов; оценка по опубликованной цене $0.0012584, биллинг не проверялся. Расходы Тимура неизвестны. Ключ только в игнорируемом локальном .env. Полная рубрика 5–7 примеров, включая противоречия и prompt injection, ожидается от Тимура.

Прочитан [официальный документ трека](https://docs.google.com/document/d/1lFWekP2SirFarATKkWa5neHNL5DlhjZKkUacvGyZk8k/edit?tab=t.0): запуск MVP, README, репозиторий/архив и демо в приложении до 5 минут. Али подтвердил дедлайн 23.09.2026 18:00 Астана (UTC+5). Ссылка на платформу подачи не указана; запрошена у Али.

## История первоначальной матрицы

Статусы ниже отражают предыдущие этапы, а не текущее состояние. Актуальные результаты приведены выше.

| Требование кейса | Доказательство | Статус |
| --- | --- | --- |
| Слабое описание → минимум 3 вопроса | Реальный вызов `/api/ai/questions` требует ключа; локально получен `503 AI_NOT_CONFIGURED` | не проверено |
| AI не выдумывает сведения | 5–7 примеров с входом, ответом и ручной оценкой | не проверено |
| Полная редактируемая карточка и ручное подтверждение | Код интерфейса перенесён; браузерный сценарий до карточки требует работающего AI | не проверено |
| Рейтинг 0–100, расшифровка, пересчёт | Локальный API тест 20→100 и разбивка; показ в браузере ещё не проверен | проверено локально, UI не проверено |
| Низкорейтинговая задача доступна | Локальный API тест: публикация 20 баллов, каталог, отклик | проверено локально |
| Сортировка и фильтры | API тест приоритета; каталог с пятью карточками и фильтрами открыт в браузере, применение фильтров не проверено | частично проверено |
| Любая команда предлагает идею, план, срок, ссылку | Локальный API тест: два предложения | проверено локально |
| Бизнес выбирает одну, несколько или ни одной | Локальный API тест: два предложения со статусом selected | частично проверено |
| Подтверждённый этап начисляет баллы разово | Последовательные повторы и 8 одновременных HTTP-запросов после исправления: команде ровно +10 | проверено локально |
| Данные по 5 каждого вида | Локальный API тест: 5 задач и 5 команд, сид содержит 5 черновиков/откликов | частично проверено |
| Ошибки: некорректный ввод, отсутствие ключа, сбой/тайм-аут AI | HTTP: короткий черновик 422, отсутствие ключа 503 и понятное сообщение в UI; тест: неверный URL 422 и недоступная модель 503; тайм-аут не проверен | частично проверено |
| README и запуск из свежего клона | Свежий `git clone` main `db9656b`, Windows Python 3.12.14, новая `.venv`, установка зависимостей, unittest, Uvicorn с `--reload`, HTTP и главная страница в браузере | проверено локально |
| Демо ≤5 минут и сдача | Таймер, актуальный коммит `main`, ссылка/архив | не проверено |

Рубрика AI на 5–7 примерах: вопросы уместны (да/нет), не повторяются (да/нет), минимум 3 (да/нет), карточка соответствует входу (да/нет), нет выдуманных фактов (да/нет), результат проходит схему (да/нет). Проверить также попытку в пользовательском тексте изменить инструкцию модели; описать ограничения, не утверждать абсолютную защиту. README владелец — Али, демо владелец — Дастан. После каждой интеграции Али отмечает точный коммит и проведённую проверку.

## Локальная интеграция 23.09.2026

- Архив `hackalem-mvp.zip`: 28 файлов побайтно совпали с незакоммиченным содержимым исходной папки; для сохранения её изменений интеграция выполнялась в отдельном worktree `ali/server` от `origin/main` (`abbadb4`).
- Windows Python 3.12.14: зависимости установлены в `.venv`; `python -m unittest discover -s tests -v` — 3 теста прошли. Проверены серверный рейтинг 20→100, публикация при 20 баллах, видимость в каталоге, два отклика, выбор двух команд, однократное начисление 10 баллов, неверный URL и ответы AI без ключа и при недоступной модели.
- Uvicorn на порту 8000: главная страница HTTP 200, `/api/health` вернул `ok`; короткий черновик получил 422 `VALIDATION_ERROR`, запрос AI без ключа — 503 `AI_NOT_CONFIGURED`. В браузере открыты главная страница и каталог; видна синтетическая карточка 30/100, отсутствие ключа показано понятным текстом.
- `OPENAI_API_KEY` и локального `.env` не обнаружено. Реальные вопросы, реальная карточка, отсутствие выдуманных сведений в ответе модели и полный браузерный сценарий от AI до решения бизнеса не проверены. Заглушка AI не применялась.

## Проверка свежего клона 23.09.2026

- Базовый SHA (`git rev-parse main`): `db9656b13714d401ed65adbe56deb884040a2902`. Выполнен отдельный `git clone` с GitHub; исходная рабочая папка с незакоммиченными изменениями не изменялась.
- На хосте нет `py`/`python` в PATH: `.venv` создана полным путём к установленному Python 3.12.14. Установка из `requirements.txt` и `pip check` успешны. В исходном клоне прошли 3 теста, после исправления — 4. Есть предупреждение Starlette об устаревшем использовании httpx в TestClient; ошибок тестов нет.
- Uvicorn запущен по README с `--reload --port 8000`. Главная страница открыта в браузере. HTTP `/api/health`: 200, `{"status":"ok"}`; отдельную вкладку health встроенный браузер заблокировал (`ERR_BLOCKED_BY_CLIENT`).
- До создания тестовых записей SQLite содержала по 5 черновиков, задач, команд и откликов. Живые HTTP-проверки: рейтинг 0 без подтверждений и 100 после подтверждения; публикация и отклики на задачу с 0 баллов; фильтры; выбор двух команд; повторное начисление; запрет этапа до выбора; неверный ввод. После остановки и нового запуска сервера записи и баллы сохранились, seed не продублировался.
- Обнаружена гонка: 8 одновременных подтверждений одного этапа начислили 70 баллов. Исправлена блокировкой записи SQLite до чтения состояния в подтверждении этапа и изменении решения. Повторная HTTP-проверка: 8 ответов 200, ровно +10 баллов; отклонение после этапа — 409. Добавлен серверный регрессионный тест.
- Без ключа оба AI-маршрута вернули 503 `AI_NOT_CONFIGURED`. Реальные AI-запросы остаются за Тимуром, полный браузерный сценарий — за Дастаном; их результаты здесь не подтверждены.

## Интеграция веток 23.09.2026

- После `git fetch origin` исправление `ali/server` (`2c188ca`) перенесено fast-forward в удалённую `main`; 4 серверных теста прошли.
- Изучен отчёт `docs/status-timur.md` в `timur/ai` (`3275f04`): реальные вызовы отсутствуют, расходы $0. Изменения только в области Тимура. Ветка объединена без конфликтов коммитом `3b52847`; повторно прошли 4 серверных и 8 AI-тестов с тестовыми подменами. Этот коммит отправлен в `main`.
- Дополнительная проверка реального типа исключения SDK через тестовую подмену обнаружила несоответствие: два `openai.APITimeoutError` приводят к `AI_UNAVAILABLE`, хотя контракт требует `AI_TIMEOUT`. Исправление включено в задание для пересылки Тимуру; `app/ai/` Али не менял. Внешних запросов при этой проверке не было.
- `dastan/ui` при проверке удалённых ссылок отсутствовала. Полный браузерный путь и реальные AI-ответы по-прежнему не подтверждены. Подготовлен пустой локальный `.env`, исключённый из Git; ожидается настройка ключа владельцем.
- По часам Windows начало проверки интеграции — 14:03 по Астане (UTC+5). Плановый ориентир окончания — 18:00 от старта 13:00. Официальная страница подачи и точный дедлайн ещё не предоставлены; фактическая подача не выполнена.
