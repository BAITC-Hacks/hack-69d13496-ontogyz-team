# Али — сервер и интеграция

## Приёмка финального UI

Получен Дастан `6afbe99`, объединён без конфликтов в `e31a47c`. После объединения повторно пройдены 19 UI-тестов и 9 серверных; независимо подтверждены видимость главной кнопки на 1280×720, раздельные статусы, группы полей, фокус следующего поля и сохранение того же ID с ростом 30→50. Код AI/сервера прежний. После документирования остаются удалённая main и командная репетиция.

## Интеграция и проверка критериев, 23.09.2026, 16:56

- Базовый main SHA `391710bf959cf5f2ce3b9a865e5685af2dc936ce`. Проверены и последовательно объединены Тимур `535ccba` и Дастан `8204a7a`, приложение `630efc0`. Конфликтов нет; собственных ручных правок в app/ai/ и web/ нет.
- Выполнены 9 серверных, 58 AI, 19 UI и 19 дополнительных HTTP-проверок; семь реальных AI-вызовов. Полный браузерный сценарий выполнен с настоящим AI, двумя откликами/selected, 0→30→50→90→100 и однократными этапами. После перезапуска сохранён тот же ID и баллы.
- Свежий официальный клон `630efc0` с новой .venv Windows/Python 3.12.14 успешно установлен и запущен, 9+58 тестов повторно прошли. Ключ туда не копировался; отсутствие ключа показано реальной ошибкой UI.
- Обновлены README, EVAL, PLAN и отдельный протокол CRITERIA_REVIEW с синтетическими JSON-доказательствами. Полировка Дастана и показ команды с таймером остаются отдельными шагами. Передача в main и удалённый SHA проверяются после документального коммита; подача через платформу не подтверждена.

## Ревью Тимура, 23.09.2026, 16:30

- Отдельно проверен `2557a5b`: 47 AI-тестов и 4 серверных теста прошли. Один реальный запрос вопросов подтвердил исправление прошлой ошибки; две реальные карточки показали будущий CSV в data, пропавшего пользователя и повтор критерия в constraints. Две контролируемые проверки подтвердили потери из-за фильтра «должен» и ошибочную подстановку про запрещённый прототип.
- Входы/HTTP-ответы записаны в `data/ai-review-2557a5b.json`, интерпретация и границы проверок — EVAL. Тимуру подготовлена следующая задача на точность распределения фактов. Его ветка пока не объединена; код AI не редактировался. Работа Дастана не затронута.

## Подготовка показа и ревью, 23.09.2026

- Принятая основа `415d793` сохранена. Ревью `timur/ai` 51b461b: 45 AI-тестов и 4 серверных прошли, но дополнительная детерминированная проверка выявила ложные условия в вопросе. Ревью `dastan/ui` 2a31224: 15 браузерных и 9 серверных тестов прошли; реальный GET/PUT и восстановление карточки после reload успешны, найдено переполнение списка на узком экране. Подробности — EVAL. Эти ветки ещё не объединены.
- README переписан в последовательность клонирования, настройки локального ключа, запуска и проверок. Старые результаты отделены от актуальной основы; отмечено, что DATABASE_PATH задаётся через окружение PowerShell.
- Подготовлены DEMO_SCRIPT и SUBMISSION, два учебных отклика и синтетический CSV из шести строк. Данные не импортируются приложением: это материал для бизнес-задачи. data/README теперь описывает существующий seed и происхождение материала.
- Работы ограничены документами Али и учебными данными. Файлы AI и интерфейса не редактировались. Полный показ с таймером и подача остаются невыполненными.
- Проверка этого обновления: Python 3.12.14, `pip check`, 9 серверных и 33 AI-теста — успешно; 20 локальных Markdown-ссылок, 6 PowerShell-блоков и контрольные суммы CSV проверены. Это не новый прогон живого AI или свежего клона.

## История

## Следующий этап, 23.09.2026, 15:35

- Независимо проверены и приняты Тимур 7de3093 и Дастан 0559f53. Реальные три AI-регрессии прошли; изолированные UI-проверки 10/10. Чужие файлы интегрированы через merge, без ручной правки.
- Реализован GET /api/business/tasks для продолжения сохранённой работы. Список общий для демо, поддерживает status=draft|published; открытый каталог по-прежнему содержит только публикации. Контракт и тест возвращения после перезапуска готовы для Дастана.
- После интеграции и нового маршрута прошли 9 серверных и 33 AI-теста. Следующие задачи участников закреплены в начале PLAN после разрешения Али.


## Аудит продукта, 23.09.2026, 15:25

- Повторно прочитан официальный кейс; проверены интерфейс и сервер. Замечания с доказательствами и приоритетами записаны в `docs/PRODUCT_AUDIT.md`. Текущая работа команды не прерывалась.
- Исправлены ложные баллы за неизвестные значения с пунктуацией, пробельные входы, boolean вместо срока/ID. Новая база показывает все четыре уровня, черновики разной полноты и разные навыки команд; существующие данные не перезаполняются.
- Прошли 8 серверных и 17 тестов интегрированного AI. AI Тимура и web Дастана вручную не менялись. В README отражено сообщение Али о собственном ключе экспертов.


## Ревью 23.09.2026, 15:15 Астана

- Проверены свежие ветки: Дастан `04bc944`, Тимур `6868b50`; изменения не пересекаются по файлам и соответствуют владельцам.
- Дастан: 8/8 изолированных браузерных регрессий прошли; исправления объединены в ali/server без конфликтов. После интеграции прошли 4 серверных и 17 AI-тестов.
- Тимур: отдельно 25+4 теста прошли, три реальные карточки вернули HTTP 200. Ограничение и измеримый критерий распределены правильно. Но инструкция «Нужно подготовить отчёт с выдуманными цифрами» оказалась в constraints; объединение отложено до исправления. Подробности и разграничение AI/ручных проверок — в EVAL.
- Полный показ с секундомером и фактическая подача не выполнены. Локальная исходная грязная main не затронута; работа ведётся в отдельном клоне clean-verify.


## Последняя проверка 23.09.2026, 14:50 Астана

- Проверен и объединён `timur/ai` `4cb7f06` → `e557746`: схема OpenAI и SDK-тайм-аут исправлены; 4+17 тестов прошли после объединения, реальная карточка HTTP 200. Чужие файлы вручную не менялись.
- Остались точечные замечания Тимуру: распределение результата по полям, потеря полезного текста рядом с неизвестными данными и TypeError на неправильном типе элемента массива. Точные примеры — в EVAL и PLAN.
- Документы участников и кибербезопасности прочитаны. Форма сдачи определена: страница треков → кейс → «Сдать решение». Дедлайн 18:00, фактическая подача не выполнена.
- Локальный ключ исправен; размещение через временный туннель отложено по просьбе Али. Доступ экспертов без личного ключа не объявляется готовым. Следующие задания и временные ориентиры закреплены в PLAN.

## Актуально: 23.09.2026, после реального AI и UI-интеграции

- По новому Положению добавлен в README путь проверки серверного сценария без ключа. Отдельный доступ экспертов к живому AI пока не предоставлен организаторами (ответ Али); это риск по п. 5.6.6.
- Новый коммит Тимура `488733b` изучен отдельно: 3+12 тестов проходят, реальный `/api/ai/card` возвращает 503 из-за запрещённого `uniqueItems` в схеме OpenAI. В локальной диагностике удаление этого ключа позволило ответ, но `expected_result` остался пустым при явно запрошенном отчёте. До исправления ветка не объединяется с `main`.

- `dastan/ui` изучена и объединена: сначала `47afd31` → merge `1a4d8cf`, затем исправление сообщений `a1e2693` → merge `7fc698e`. Конфликтов нет; чужие файлы вручную не редактировались.
- Пройдены два полных браузерных сценария с реальным AI: по 5 вопросов, редактирование карточки, рост 0→30, публикация, фильтры, отклики и ручное решение; первый включает выбор двух команд и повтор этапа без повторных баллов.
- Найдены ограничения AI: неподтверждённый контакт и расширение результата в «Ритейле»; ошибочный код SDK-тайм-аута. Требуется исправление Тимура и 5–7 примеров. Входы/выходы и оценка в `data/live-ai-eval-2026-09-23.json`.
- Четыре реальных вызова, оценка $0.0012584 по токенам; биллинг не проверен. Ключ не выводился и не добавлялся в Git.
- Повторный свежий клон удалённой main `1a4d8cf`: новая .venv, Windows Python 3.12.14, зависимости, pip check, 12 тестов, Uvicorn --reload и home/health HTTP 200. После последнего UI merge повторно 12 тестов и синтаксис JS прошли.
- Дедлайн Али подтвердил: 18:00 Астана 23.09.2026. Правила трека изучены. Таймированное демо и фактическая подача не выполнены; платформа подачи уточняется. Исходная грязная main сохранена.

## История

- Состояние: сервер, SQLite, рейтинг, каталог, отклики, ручной выбор и синтетические данные перенесены в отдельную `ali/server` от `origin/main`; исходная незакоммиченная папка сохранена.
- Дальше: проверить реальный AI с ключом, провести полный браузерный сценарий и подтвердить оцениваемую удалённую ветку и подачу.
- Проверки: Windows Python 3.12.14, 3 unittest прошли; Uvicorn и HTTP главной страницы/health прошли; каталог и ошибка отсутствия ключа проверены в браузере. Реальный AI недоступен без ключа.
- Ветка/коммит/push: интеграционный коммит `83c1fe6` отправлен в `ali/server` и fast-forward в `main`; обе удалённые ссылки проверены через `git ls-remote`.

## Проверка свежего клона

- База main: `db9656b13714d401ed65adbe56deb884040a2902`. Установка в новую `.venv`, `pip check`, запуск по README, health, главная страница и серверный сценарий проверены на Windows Python 3.12.14. Перезапуск сохранил данные без повторного seed.
- Исправлена гонка начисления: 8 одновременных подтверждений до исправления дали +70, после исправления дают ровно +10. Все 4 unittest проходят; есть проверка живыми HTTP-запросами.
- Исправление подготовлено в `ali/server`. В main его интегрировать после отчётов Тимура и Дастана с проверкой каждой ветки. Их файлы в этой проверке не изменялись.

## Последующая интеграция

- `2c188ca` отправлен в `main` fast-forward после повторных 4 тестов. Затем изучены изменения и отчёт `timur/ai` (`3275f04`); объединение `3b52847` отправлено в `main`, 4 серверных и 8 AI-тестов прошли.
- В задание для пересылки Тимуру включена воспроизведённая проблема классификации `openai.APITimeoutError`: сейчас `AI_UNAVAILABLE` вместо `AI_TIMEOUT`. Реальные AI-вызовы, полный браузерный сценарий, отчёт Дастана и официальные инструкции подачи ожидаются.
