# AI Sana Challenge Hub — Ontogyz

MVP для превращения короткой бизнес-задачи в подтверждённую карточку с прозрачным рейтингом готовности. Команды видят общий каталог, откликаются, а бизнес вручную решает, с кем работать.

## Что работает

Бизнес вводит описание, получает вопросы AI, редактирует карточку, отмечает подтверждённые сведения, видит рейтинг и публикует задачу. Студенты смотрят каталог и направляют предложения. Бизнес самостоятельно выбирает или отклоняет несколько откликов и подтверждает этап с однократным начислением 10 баллов. Запуск AI зависит от действительного ключа и доступа к модели; без них интерфейс показывает явную ошибку. Приложение сохраняет записи в SQLite между запусками.

## Запуск в Windows PowerShell

```powershell
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
```

Открыть http://127.0.0.1:8000 и http://127.0.0.1:8000/api/health. После сообщения `Uvicorn running` оставить терминал открытым и работать в браузере; `Ctrl+C` останавливает сервер. Для AI создать `.env` по `.env.example` и поместить ключ только туда. Если `py` отсутствует, заменить на `python`. Модель `gpt-4.1-mini-2025-04-14` проверена реальными запросами 23.09.2026; доступ другого аккаунта проверяется отдельно. Переменная окружения `DATABASE_PATH` необязательна, база по умолчанию в `data/hackalem.db` и не добавляется в Git.

Для проверки API: `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`.

Проверки AI-модуля без внешних запросов: `.\.venv\Scripts\python.exe -m unittest app.ai.test_service -v`. Эти тесты используют подмену клиента и не подтверждают доступ к модели или качество реальных ответов. Если ни `py`, ни `python` не доступны в PATH, создайте `.venv` через полный путь к установленному Python: `& 'C:\путь\к\python.exe' -m venv .venv`.

### Проверка экспертом без личного API-ключа

По сообщению Али эксперты используют собственный API-ключ. Для живого AI перед запуском создайте локальный `.env` из `.env.example` (существующий файл не перезаписывайте), укажите свой `OPENAI_API_KEY` и доступный вашему аккаунту `OPENAI_MODEL`, затем запустите или перезапустите сервер. Ключ команды в репозиторий не входит. Следующий сценарий доступен также без ключа.

После чистого запуска проверьте `/api/health` и откройте главную страницу. В разделе «Команда» видны пять синтетических опубликованных задач; фильтр уровня «Черновик» показывает задачи с низким рейтингом. Откройте такую задачу, выберите команду, заполните идею и план (каждое не короче 10 символов), срок от 1 до 365 дней и URL вида `https://example.org/demo`, затем отправьте отклик. В разделе «Отклики» выберите эту задачу, вручную выберите предложение и подтвердите этап: команда получит 10 баллов, повторная кнопка станет недоступна. Исходные записи и баллы сохраняются в SQLite. Серверные проверки запускаются указанными выше командами.

Без `OPENAI_API_KEY` сервер возвращает `AI_NOT_CONFIGURED`, а интерфейс показывает понятное сообщение о ключе: живой AI в таком запуске недоступен. Два реальных примера работы модели и ручная оценка качества сохранены в `data/live-ai-eval-2026-09-23.json`. Эксперт настраивает собственный ключ на компьютере, где запущен сервер. Секреты не включены в репозиторий или браузер.

## Архитектура

Один процесс FastAPI отдаёт HTML/CSS/JavaScript и JSON API. SQLite хранит задачи, команды, отклики и подтверждённые этапы. Сервер рассчитывает рейтинг из подтверждённых полей; AI задаёт вопросы и помогает составить редактируемую карточку. Схемы и примеры: `docs/CONTRACTS.md`.

Формула: контекст 10, потребность 10, данные 20, результат 15, критерии успеха 15, ограничения 10, пользователи 10, контакт 5, взаимодействие 5. Только заполненные и подтверждённые поля. Уровни: 0–39 черновик, 40–69 рабочая, 70–89 готовая, 90–100 приоритетная. Каталог сортируется по убыванию рейтинга, затем по id. Задачи 0–39 видны и доступны для отклика. Контент полей и URL проверяются при приёме сервером. Статический демо-переключатель ролей не представляет собой настоящую авторизацию.

## Тестовый сценарий

Новая база включает пять карточек с баллами 30, 50, 65, 75, 90, пять черновиков разной полноты и пять команд с разными навыками. Все четыре уровня каталога можно проверить сразу. Данные и условия вымышлены для демонстрации. Обновление кода не меняет существующую базу; для отдельной чистой репетиции задайте новый путь, например `$env:DATABASE_PATH = "data/demo-rehearsal.db"`, перед запуском сервера. Не удаляйте рабочую БД ради сброса демонстрации.

1. Запустить приложение и проверить `/api/health`. При первом запуске автоматически создаются 5 синтетических черновиков, 5 карточек, 5 профилей команд и 5 предложений. Их источник — команда Ontogyz, они служат только демонстрации.
2. Бизнес вводит «В магазине много списаний продуктов. Хотим сократить их», тему «Ритейл». Получает ≥3 вопроса от API, отвечает на вопросы, редактирует карточку и отмечает только подтверждённые поля.
3. Сохраняет карточку, видит разбивку баллов, дополняет сведения и видит рост рейтинга. Публикует задачу; задача с низким рейтингом тоже видна.
4. В каталоге переключает тему и уровень, открывает задачу. Выбирает команду и отправляет идею, план, срок и действительный URL.
5. Бизнес переходит к откликам, выбирает или отклоняет предложения по одному; может выбрать несколько. Подтверждает этап выбранной команды, баллы начисляются один раз.

## Демо до 5 минут и ограничения

Аудит продукта: `docs/PRODUCT_AUDIT.md`; актуальные задачи — начало `docs/PLAN.md`. После интеграции Тимура `7de3093`, Дастана `0559f53` и API сохранённых задач проходят 9 серверных и 33 AI-теста. «Не знаю.» и «Нет данных!» не дают баллов; пробельные запросы отклоняются до вызова AI. Три реальные AI-регрессии Али прошли; все ограничения и история — в `docs/EVAL.md`. Серверный список `GET /api/business/tasks` уже готов, интерфейс продолжения редактирования ещё разрабатывается.

Проверены два браузерных сценария с реальным AI, редактированием, ростом рейтинга 0→30, публикацией и решением бизнеса. Первые ошибки и ручные исправления сохранены в `data/live-ai-eval-2026-09-23.json`. После них интегрировано исправление Тимура: девять полей собираются из исходных фрагментов, контакт проверяется отдельно, тайм-аут SDK возвращает 504. Проходят 4 серверных и 17 AI-тестов; отчёт Тимура содержит шесть реальных примеров, Али отдельно повторил реальную карточку. Ошибки распределения фактов по полям и потери полезного текста рядом с неизвестными сведениями ещё требуют исправления; проверка человеком обязательна. Подробности: `docs/EVAL.md`, `docs/status-timur.md`. Указанные там расходы оценены по токенам, биллинг аккаунта не проверялся.

Официальный [документ трека](https://docs.google.com/document/d/1lFWekP2SirFarATKkWa5neHNL5DlhjZKkUacvGyZk8k/edit?tab=t.0) требует запускаемого MVP, README и демонстрации в приложении до 5 минут. Дедлайн 23.09.2026 в 18:00 по Астане (UTC+5). По [инструкции участников](https://drive.google.com/file/d/105Rnhzg3Q5tKjfGZIddRqY13kmq_w4r_/view), после push нужно на edu.astanahub.com открыть страницу треков, выбрать кейс, нажать «Сдать решение» и заполнить название и описание проекта; обновление доступно до дедлайна. Таймированный прогон и фактическая подача пока не выполнены. Перед сдачей проверить оцениваемую удалённую `main`, доступ жюри и подтверждение приёма материалов. Внешняя демо-ссылка пока отсутствует.

Демо: шаги 2–5 по одной подготовленной задаче, с показом роста рейтинга, низкорейтинговой карточки и ручного решения. Состояние AI и проверенные примеры заносятся в `docs/status-timur.md`, остальные подтверждения — в `docs/EVAL.md`. AI может ошибаться, поэтому бизнес редактирует и подтверждает текст. Контакт и сведения задач в демонстрационных данных вымышлены. Полноценной регистрации, управления правами, защиты бизнеса от чужого редактирования и производственного развёртывания нет.

Внешние материалы: [FastAPI](https://github.com/fastapi/fastapi), [Uvicorn](https://github.com/encode/uvicorn), [OpenAI Python SDK](https://github.com/openai/openai-python) и их зависимости устанавливаются из `requirements.txt`; внешняя модель — OpenAI `gpt-4.1-mini-2025-04-14`. Реальные данные организаторов не применялись. Вклад команды: Али — сервер, хранение, рейтинг и интеграция; Тимур — AI, промпты и оценка; Дастан — веб-интерфейс и демо. Этот локальный собранный вариант подготовлен совместно с Codex; фактические персональные вклады уточнить перед сдачей.
